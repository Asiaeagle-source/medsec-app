-- ============================================================
-- 22_stockless_alert.sql — SUPERSEDED(已作廢)
-- ============================================================
-- 此檔內容已全部合併進 sql/v2/23_inventory_consolidated.sql。
-- 原本 19(CREATE OR REPLACE VIEW)與 20(DROP COLUMN 但 view 仍相依)
-- 的依賴順序會造成 ERROR(view depends on column / 42P16)。
--
-- 保留此空檔僅為維持遷移編號連續、避免外部引用失效。
-- 不要在此加任何 DDL;一切以 23 為準。請直接執行到 23。
-- (純註解檔,執行結果為 Success / No rows returned)
-- ============================================================
